<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="TaskMaster.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:TaskMaster.Views"
    Shell.FlyoutBehavior="Disabled">

    <ShellContent
        Title="Inscription"
        ContentTemplate="{DataTemplate views:Inscription}"
        Route="Inscription" />

    <ShellContent
        Title="Connexion"
        ContentTemplate="{DataTemplate views:Connexion}"
        Route="Connexion" />

    <ShellContent
        Title="Dashboard"
        ContentTemplate="{DataTemplate views:Dashboard}"
        Route="Dashboard" />

</Shell>
